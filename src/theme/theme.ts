export const themes = {
    light: {
      background: "#f7f7f7",   // Fondo principal
      surface: "#F8F9FA",      // Para cards y contenedores
      text: "#202124",         // Texto principal
      textInverted: "#FFFFFF", // Texto invertido
      textSecondary: "#5F6368", // Texto secundario
      primary: "#0B3759",      // Azul oscuro (principal, títulos)
      secondary: "#457B9D",    // Azul (secundario, botones) #AB0800 Rojo cesmag
      accent: "#F4A261",       // Naranja cálido (acentos, etiquetas)
      success: "#2A9D8F",      // Verde (confirmaciones, éxito)
      warning: "#E9C46A",      // Amarillo (advertencias)
      error: "#D62828",        // Rojo oscuro (errores)
      inputBg: "#F0F0F0",      // Fondo de inputs
      borderColor: "#DDDDDD",  // Bordes suaves
    },
    dark: {
      background: "#121212",   // Fondo principal oscuro
      surface: "#1E1E1E",      // Para cards y contenedores
      text: "#EDEDED",         // Texto principal
      textInverted: "#121212", // Text
      textSecondary: "#A0A0A0", // Texto secundario
      primary: "#FF6B6B",      // Rojo vibrante
      secondary: "#48BB78",    // Verde secundario
      accent: "#FFA85C",       // Naranja cálido
      success: "#6FCF97",      // Verde éxito
      warning: "#F2C94C",      // Amarillo advertencia
      error: "#FF5252",        // Rojo errores
      inputBg: "#222222",      // Fondo de inputs
      borderColor: "#333333",  // Bordes más oscuros
    },
  };
  